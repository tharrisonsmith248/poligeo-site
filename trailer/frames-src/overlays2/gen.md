# Overlay chrome v2 (for the Kling 2.5 combat clips)

Files here are transparent 1920×1080 chrome layers composited over the graded
live-action in ffmpeg. Ticker text lives in separate `tk-*.html` strips that
are rendered once and scrolled with an ffmpeg overlay expression, so the
marquee moves even though the chrome is a still.
