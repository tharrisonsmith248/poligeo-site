# poligeo-site

The landing page served at **poligeo.org**, hosted on GitHub Pages.

`index.html` is a single self-contained file (styles and images inlined), so it
deploys anywhere with no build step. It is generated — edit the source in the
Polaris repo (`site/poligeo.src.html`), run `site/build.py`, and copy the built
file here.
